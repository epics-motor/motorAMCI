# Use the following lines if motorAMCI was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorAMCI was built outside motor
#!MOTOR_AMCI=
#!-include $(MOTOR_AMCI)/configure/RELEASE.local
